#include<stdio.h>

struct node{
	int data;
	node* link:
};
node*a;
a= null;
node*temp=(node*)malloc(size of(node));
(*temp).data = 2;
(temp*).link = null;
a =temp;
temp=new node();
(*temp).

